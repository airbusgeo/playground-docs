{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "title": "job-object-detection-input",
    "description": "Geo Processes Manager API input schema for tile change detection",
    "type" : "object",
    "required": [
        "name",
        "sourceId1",
        "sourceId2",
        "geom"
    ],
    "properties": {
        "name": {
            "description": "Job name",
            "type": "string"
        },
        "sourceId1": {
            "description": "Image 1 source ID",
            "type": "string"
        },
        "sourceId2": {
            "description": "Image 2 source ID",
            "type": "string"
        },
        "geom": {
            "description": "Area of interest",
            "type": "object",
            "required": [
                "type",
                "coordinates"
            ],
            "properties": {
                "type": {
                    "description": "Always a Polygon",
                    "type": "string",
                    "default": "Polygon"
                },
                "coordinates": {
                    "description": "GeoJSON Polygon coordinates",
                    "type": "array"
                }
            }
        },
        "datasetId": {
            "description": "Dataset ID, if the dataset already exists (exclusive with datasetName)",
            "type": "string"
        },
        "datasetName": {
            "description": "Dataset name, to create a new dataset (exclusive with datasetId)",
            "type": "string"
        }
    },
    "oneOf": [
        {
            "required": [
                "datasetId"
            ]
        },
        {
            "required": [
                "datasetName"
            ]
        }
    ]
}
